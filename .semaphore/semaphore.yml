version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Yarn Install
    task:
      jobs:
        - name: install
          commands:
            - checkout
            - yarn install
    dependencies: []
  - name: Build
    dependencies:
      - Yarn Install
    task:
      jobs:
        - name: build project
          commands:
            - yarn build
